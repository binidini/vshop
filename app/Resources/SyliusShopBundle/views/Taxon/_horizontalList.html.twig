<div class="ui two column stackable grid">
    <div class="row">
        {% for taxon in taxons %}
        <div class="column">
            {% include '@SyliusShop/Taxon/_box.html.twig' %}
        </div>
        {% if 0 == loop.index % 2 %}
    </div>
    <div class="row">
        {% endif %}
        {% endfor %}
    </div>
</div>
